<article class="appears">
    <slot/>
</article>

<style>
    .appears {
        opacity: 1;
        animation: appears ease-out forwards;
        animation-timeline: view();
        animation-range: entry cover 25%;
    }
    @keyframes appears {
        from {
            opacity: 0;
            transform: translateX(100%);
        }
        to {
            opacity: 1;

        }
    }
</style>
